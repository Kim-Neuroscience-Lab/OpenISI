# Build artifacts
target/
*.exe
*.dll
*.pdb

# Vendored third-party artifacts
vendor/

# Dev debugging output (generated figures)
dev_figures/

# Data files
*.oisi
*.h5
*.hdf5
*.mat

# ...EXCEPT the committed SYNTHETIC e2e fixtures (tiny, deterministic, no real
# data) — they let the pipeline equivalence/incremental/schema gates run on CI.
# Real data (R43, rig captures) stays ignored and is never published.
!crates/isi-analysis/tests/fixtures/synthetic/
!crates/isi-analysis/tests/fixtures/synthetic/*.oisi

# Image files (data output, not source assets)
*.png
!src-tauri/icons/*.png
!src-tauri/icons/*.ico

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Tauri build artifacts
src-tauri/WixTools/
src-tauri/gen/

# Godot editor artifacts (legacy, not part of 1.0)
.godot/
*.import

# Legacy directories (not part of Rust rewrite)
/bin/
/build/
/daemon/
/dist/
/extension/

# Runtime data
data/
*.log

# Local config overrides
settings.local.json

# Test data
test_data/

# Claude artifacts
.claude/

# External reference codebases (MATLAB/Python pipelines we port from) — not tracked
/reference/

# Editor/backup scratch files (never commit)
*.bak
*.orig

# Python bytecode (golden-fixture generators are run, not byte-compiled into VCS)
__pycache__/
*.pyc

# Transient persisted session state (resumability) — per-machine runtime, not source
config/dev/session.json
